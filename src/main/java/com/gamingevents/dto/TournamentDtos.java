package com.gamingevents.dto; import com.gamingevents.entity.TournamentStatus; import jakarta.validation.constraints.*; import java.math.BigDecimal; import java.time.Instant; public final class TournamentDtos { private TournamentDtos(){} public record Request(@NotBlank String name,@NotBlank String gameName,String description,@NotBlank String format,@DecimalMin("0") BigDecimal entryFee,@Min(2) int maxPlayers,@Future Instant startDate){} public record Response(Long id,String name,String gameName,String organizer,BigDecimal entryFee,int maxPlayers,Instant startDate,TournamentStatus status){} }
